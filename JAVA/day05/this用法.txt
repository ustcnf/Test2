This 两个用法：
1.当定义类中功能时，该函数内部用到调用该函数的对象时，用this代表这个对象。
  但凡本类功能内部使用了本类对象，都用this表示。
  
 2.用于构造函数间互相调用；this语句只能放在构造函数的第一行( 初始化动作先执行）。
 构造函数间调用格式：this(name)  == p(name)
  eg:this.age = age;
     this(name);